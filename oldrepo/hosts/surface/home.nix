{ config, lib, pkgs, ... }:

{
   # home.username = "yarn";
   # home.homeDirectory = "/home/yarn";
   # home.stateVersion = "25.11";

   # config = {
   #     userSettings = {
   #        git.enable = false;
   #        hyprland.enable = false;
   #        sh.enable = false;
   #        vim.enable = false;
   #     };
   # };
}
